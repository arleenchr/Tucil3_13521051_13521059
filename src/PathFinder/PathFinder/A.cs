using System;

namespace PathFinder
{
    public class A
    {
        /* attributes */
        Graph graph;
        Vertex start;
        Vertex end;

        /* ctor */
        public A(Graph g, Vertex s, Vertex e){
            graph = g;
            start = s;
            end = e;
        }

        /* method */


    }
}